/**
 * 文字列に新しい行を連結します。末尾に改行が無い場合だけ改行を追加します。
 * @param base
 * @param newLine
 */
export declare function appendLine(base: string, newLine: string): string;
/**
 * 文字列の末尾に改行がなければ追加します。
 * @param str
 */
export declare function appendReturnIfNotExists(str: string): string;
